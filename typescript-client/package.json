{
  "name": "root",
  "private": false,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "lerna": "lerna",
    "set-version": "lerna version --no-git-tag-version --no-push --yes",
    "build": "lerna run build",
    "publish-packages": "lerna publish --no-git-tag-version --no-push --yes",
    "test": "lerna run test"
  }
}
