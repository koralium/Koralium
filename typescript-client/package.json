{
  "name": "root",
  "private": false,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "lerna": "lerna",
    "set-version": "lerna version --skip-git --yes",
    "build": "lerna run build",
    "publish-packages": "lerna run publish-package"
  }
}
